#!/bin/sh
set -e

## From the environment variables replace the nginx configuration placeholders
ESCAPE='$' envsubst < /root/vhost.conf.template > /etc/nginx/conf.d/default.conf \
    && ESCAPE='$' envsubst < /root/nginx.conf.template > /etc/nginx/nginx.conf \
    && nginx -g 'daemon off;'
