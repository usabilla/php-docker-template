#!/bin/sh

set -xe

curl -sL -o /usr/local/bin/shush https://github.com/realestate-com-au/shush/releases/download/v1.4.1/shush_linux_amd64

chmod +x /usr/local/bin/shush

shush -v

RESULT=$?

exit $RESULT
